{
  "rules": {
    ".read": false,
    ".write": false,

    "shops": {
      "$shopId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "gallery": {
      "$shopId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "pushTokens": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
